%h1 Admin::Tasks#edit

= simple_form_for(resource, url: resource_url) do |f|
  = f.error_notification

  - attrs_for_form.each do |attr|
    = f.input attr

  = f.button :submit

%p Find me in app/views/admin/tasks/edit.html.haml
