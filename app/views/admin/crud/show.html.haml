%h1
  #{resource.class} #{resource.id}
%p
  = link_to t('.edit'), edit_resource_path
  \|
  = link_to t('.home'), collection_url

%table
  %thead
    %th key
    %th value

  %tbody
    - resource.attributes.sort.each do |key, value|
      %tr
        %td= key
        %td= value

%p Find me in app/views/crud/show.html.haml
